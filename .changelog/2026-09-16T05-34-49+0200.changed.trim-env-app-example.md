# Trim `.env.app.example`

Removed Better Auth, DB, SMTP, and OAuth placeholders that belong to a specific cloned app, not this orchestrator. The example now documents passthrough-only usage and drops unused `BETTER_AUTH_TELEMETRY*` defaults from the Dockerfile.
