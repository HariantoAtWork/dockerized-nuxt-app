Git graph commit nodes now align with the commit title line instead of the vertical centre of each row.

This keeps the node visually next to the subject text, which is easier to scan in taller rows with meta, refs, and status tags.
