Rebuilt the dashboard Git graph with a single SVG lane canvas, rounded elbow connectors, filled nodes, and continuous vertical spines between rows.

This replaces the segmented per-row lane blocks so branch paths read more like native Git graph tools.
