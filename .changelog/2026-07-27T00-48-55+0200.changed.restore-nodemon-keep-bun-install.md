Restore nodemon for the Nuxt server while keeping the official Bun install.

`bun --watch` did not match the expected directory-watch behaviour after `.output` rebuilds. The runner again uses nodemon on `appOutput`, still waits until `server/index.mjs` exists before starting, and the image installs nodemon globally alongside curl-installed Bun/canary.
