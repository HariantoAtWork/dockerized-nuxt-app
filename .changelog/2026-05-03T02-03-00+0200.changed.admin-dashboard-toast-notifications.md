# Orchestrator admin dashboard toasts

Replaced blocking `alert()` calls with a fixed bottom-right toast stack (success / error / info styling). Manual refresh shows a success toast only when status and logs load; rebuild/restart show completion toasts. Background polling uses silent refresh so intermittent failures do not spam toasts.
