Add `Dockerfile.platform` for multi-arch Hub pushes, matching the acmedns-stack pattern.

Bun install/build for the admin UI and orchestrator deps run on `$BUILDPLATFORM` so QEMU amd64 on Apple Silicon no longer SIGILL-crashes. `_push.yml` uses this file; local `_build.yml` keeps the native `Dockerfile`.
