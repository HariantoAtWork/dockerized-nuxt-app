`bin/docker-build.sh` now checks and reads `package.json` via `${GITHUB_REPO}/package.json` explicitly.

This avoids relying on the current working directory and ensures the CI script detection always targets the repository that was cloned or updated.
