Force `git reset --hard` after checkout so moving `.git` into an empty compose volume actually materialises the working tree (including package.json). Empty `/git` mounts now clone directly instead of the recover-via-temp path.
