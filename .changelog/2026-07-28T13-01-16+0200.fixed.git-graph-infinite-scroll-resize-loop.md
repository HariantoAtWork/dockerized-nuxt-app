Fixed the Git graph panel infinitely growing while scrolling by removing the ResizeObserver height feedback loop.

Row height now comes from commit content, while the lane SVG stretches to match without re-measuring and mutating layout.
