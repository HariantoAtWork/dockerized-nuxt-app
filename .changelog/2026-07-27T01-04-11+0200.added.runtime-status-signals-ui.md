Add runtime signal statuses for the Nuxt server entry and nodemon.

`/api/status` now reports `serverEntryExists` and `serverEntryPath`. The admin UI shows a dedicated runtime panel for build-entry and nodemon state, with the full JSON payload tucked behind a details section.
