Live Checkout Switch & rebuild now works while a commit is pinned, including on the same branch, and always clears the pin before deploying the branch tip.

This matches the expectation that choosing a branch and rebuilding returns the orchestrator to tip tracking.
