The Git graph panel now renders true multi-lane branch topology, including connector rows for fork and merge paths.

This makes the dashboard graph behaviour much closer to native Git Graph visualisation, so branch flow is easier to read at a glance.
