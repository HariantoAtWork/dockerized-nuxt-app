# Centre git graph nodes in the lane column

Lane width always reserved two character columns, so a single-lane graph sat several pixels left of centre. Width now matches the actual lane characters with equal padding, and the right divider is drawn inset so it no longer shifts the SVG.
