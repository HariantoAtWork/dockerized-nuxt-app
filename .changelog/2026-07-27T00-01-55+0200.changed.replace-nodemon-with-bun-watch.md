Replace nodemon with `bun --watch` for the Nuxt server entrypoint.

The orchestrator now runs `bun --watch .output/server/index.mjs` instead of watching the whole `.output` tree with nodemon, and the image no longer installs nodemon globally.
