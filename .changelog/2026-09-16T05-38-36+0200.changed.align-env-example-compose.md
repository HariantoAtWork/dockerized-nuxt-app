# Align `.env.example` with compose wiring

Drop unused `DOCKER_DEFAULT_PLATFORM`, document `VERBOSE_LOGGING` / `WATCH_INTERVAL_MS`, and load `.env` as an `env_file` so orchestrator settings reach the container.
