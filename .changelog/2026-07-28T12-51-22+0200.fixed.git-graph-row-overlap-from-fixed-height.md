Fixed overlapping Git graph commit rows by sizing each row from its content and measured DOM height instead of a single fixed height.

Rows with refs and status tags now expand cleanly, and the SVG lane layout is rebuilt to match those heights.
