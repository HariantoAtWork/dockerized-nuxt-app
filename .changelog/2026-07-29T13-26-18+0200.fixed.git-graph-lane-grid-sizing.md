# Size git graph lanes via CSS grid

`.lane-cell` and `.content` already sit in a two-column grid. The SVG was absolutely positioned, so the `auto` column collapsed and needed a hard-coded width. The SVG now keeps its intrinsic width from attributes, stretches to the row height, and `.content` takes the remaining space.
