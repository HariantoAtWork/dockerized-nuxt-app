The Git graph now renders lane connectors with SVG on a dark canvas so vertical lines and nodes stay visible against the dashboard background.

Commit rows always draw spine segments between nodes, fixing the case where linear histories only expose asterisks and no pipe characters.
