# Even out right padding on git graph commit rows

The commit subject text in the git graph panel had no visible spacing on the right edge, making long messages look clipped. Increased the right padding on `.content` to match the left spacing.
