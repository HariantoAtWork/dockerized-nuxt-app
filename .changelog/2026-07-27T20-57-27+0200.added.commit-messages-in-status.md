# Show git commit messages in orchestrator status

`/api/status` and the admin dashboard now include the subject line for the current and remote commits (not only SHAs). Watcher rebuild logs also print the incoming commit message.
