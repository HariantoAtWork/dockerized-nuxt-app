# Skip `bun run ci` when absent from package.json

`bin/docker-build.sh` used to always run `bun run ci`, which fails if the cloned app’s `package.json` has no `scripts.ci` entry. The build phase now checks for a `ci` script and only runs it when defined; otherwise it logs and continues (output folder wait and rest of the flow unchanged).
