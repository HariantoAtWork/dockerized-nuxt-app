Store the selected git branch under `/data` so sync cannot wipe it.

`git clean -fd` runs in `/app` (the clone). The orchestrator now defaults `GIT_BRANCH_FILE` to `/data/.orchestrator_git_branch` on the persistent data volume, and still reads a legacy `/app` file if present.
