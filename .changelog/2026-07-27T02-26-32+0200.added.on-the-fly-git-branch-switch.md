Add on-the-fly git branch switching from the admin UI.

You can list remote branches, switch to another branch, and the orchestrator checkouts, rebuilds, and restarts nodemon so the chosen branch goes live for testing. The selection is persisted under `.orchestrator_git_branch`.
