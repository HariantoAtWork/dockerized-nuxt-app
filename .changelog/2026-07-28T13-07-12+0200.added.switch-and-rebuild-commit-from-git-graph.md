Added a Switch & Rebuild action on each Git graph commit so you can pin and deploy a specific SHA from the dashboard.

The orchestrator persists the pinned commit, skips tip auto-updates while pinned, and clears the pin when switching branches again.
