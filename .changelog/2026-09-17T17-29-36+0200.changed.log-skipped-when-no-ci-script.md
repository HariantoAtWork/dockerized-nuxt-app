Log `bun run ci: SKIPPED` when the cloned app has no `scripts.ci`, then fall through to `scripts.build` as before.

Makes the missing-ci path obvious in orchestrator logs instead of only noting the build fallback.
