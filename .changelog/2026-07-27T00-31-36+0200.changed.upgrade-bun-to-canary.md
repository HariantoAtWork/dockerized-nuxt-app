Upgrade Bun to the canary channel after the official install in both image stages.

Ensures the admin UI build and runtime images use `bun upgrade --canary` so the container tracks Bun’s latest canary build.
