Fixed the Git graph lane renderer so connector lines and commit nodes are fully visible and no longer clipped by the side panel.

The graph now uses explicit lane-colour variables per branch column with dedicated node and pipe elements instead of transparent text glyphs.
