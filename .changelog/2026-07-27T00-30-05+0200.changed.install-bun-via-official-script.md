Install Bun with the official curl installer instead of `npm install -g bun`.

Both the admin UI build stage and the runtime image now use `https://bun.com/install` on Alpine (with curl/bash/unzip) and symlink `bun` into `/usr/local/bin`.
