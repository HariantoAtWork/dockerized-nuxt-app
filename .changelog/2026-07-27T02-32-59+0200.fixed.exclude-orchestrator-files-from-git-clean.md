Keep the selected branch on `/app` and leave Nuxt `/data` alone.

Default `GIT_BRANCH_FILE` is `/app/.orchestrator_git_branch` again. `git clean -fd` now excludes orchestrator state files so sync cannot wipe the persisted branch (or commit/build flags).
