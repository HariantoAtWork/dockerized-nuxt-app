Give the orchestrator its own persistent CI state directory.

State (git branch, commit records, build-complete flag) now lives under `/var/lib/orchestrator`, mounted from `./data/orchestrator`, separate from the `/app` clone and Nuxt’s `/data`.
