The admin dashboard now includes a right-side Git graph panel showing recent commit history as an ASCII graph.

This adds quick visual context for branch and commit movement directly in the Nuxt Orchestrator UI without leaving the dashboard.
