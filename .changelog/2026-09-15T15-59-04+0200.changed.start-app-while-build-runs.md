Start the Nuxt server immediately when `.output/server/index.mjs` already exists, so a previous build keeps serving while sync/build run. After a real rebuild the orchestrator still stops and starts the process so nodemon loads the complete new output.
