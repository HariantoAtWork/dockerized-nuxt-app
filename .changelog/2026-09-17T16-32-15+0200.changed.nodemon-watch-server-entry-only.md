Watch only `/app/server/index.mjs` with nodemon instead of the full `APP_OUTPUT` tree.

Avoids restart noise when the app creates or changes other files under `/app`; rebuilds still restart the process via the orchestrator.
