# - Docker Settings
DOCKER_DEFAULT_PLATFORM=linux/amd64
DOCKER_HUB_IMAGE=dockerized-nuxt-app
GITHUB_REPO_URL=

# Git branch tracked by clone/build/watch (default in image: main)
# GIT_BRANCH=main

# Orchestrator admin HTTP (maps to host 127.0.0.1:9091 when compose publishes 9091:9090)
# ADMIN_BIND=0.0.0.0
# ADMIN_PORT=9090
# ADMIN_TOKEN=
